{
  "name": "crh-create-ai",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev:web": "npm --prefix apps/web run dev",
    "dev:api": "python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "build:web": "npm --prefix apps/web run build"
  }
}
